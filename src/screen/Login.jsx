import React from "react";
import { Text, View } from "react-native";

export const Login= ()=> {
    return (
        <View style={{backgroundColor: 'blue', flex:1}}>
            <Text>OI</Text>
        </View>
    )
}